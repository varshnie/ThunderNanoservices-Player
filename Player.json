{
  "$schema": "plugin.schema.json",
  "info": {
    "title": "Player Plugin",
    "callsign": "Player",
    "locator": "libWPEFrameworkPlayer.so",
    "status": "production",
    "description": [
      "The Helloworld plugin provides functionality for streaming."
    ],
    "version": "1.0"
  },
  "configuration": {
    "type": "object",
    "properties": {
      "configuration": {
        "type": "object",
        "required": [],
        "properties": {
          "storage": {
            "type": "string",
            "description": "Filename of DataModel file (default: DataModel.json)"
          }
        }
      }
    }
  }
}
